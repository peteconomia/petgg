config <- list(
	base_size = 12,
	base_family = "Helvetica",
	base_line_size = 0.5,
	base_rect_size = 0.5,
	axis_title = F,
	legend_position = "bottom",
	legend_direction = "horizontal",
	legend_title = F
)
